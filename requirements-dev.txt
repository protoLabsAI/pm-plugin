# Test-only deps. At runtime fastapi + langchain-core are provided by the host
# (protoAgent); these let the suite run standalone in CI.
fastapi>=0.110
langchain-core>=0.2
pyyaml>=6
httpx>=0.27
pytest>=8
